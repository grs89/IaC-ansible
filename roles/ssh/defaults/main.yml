---
# Valores por defecto para el role ssh
sshd_config_file: /etc/ssh/sshd_config
sshd_config_path: "{{ sshd_config_file }}"
ssh_service_name: sshd

ssh_permit_root_login: 'no'
ssh_port: 22
ssh_disable_password_auth: true


